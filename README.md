# emoji_frontsystem

表情包档案馆管理后台（Next.js App Router）。

## 开发

```bash
npm install
npm run dev
```

打开 `http://localhost:5818`。

## 环境变量

在项目根目录创建 `.env.local`（已在 `.gitignore`，不要提交敏感信息）：

```bash
NEXT_PUBLIC_API_BASE=http://localhost:5050/api
```

## 构建与启动

```bash
npm run build
npm run start
```
